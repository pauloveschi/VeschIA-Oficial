# VeschIA — Site Oficial

Hub de links da marca VeschIA, apontando pros produtos (hoje: AIP).

## Rodar local
```
npm install
npm run dev
```

## Deploy
Framework: Vite (React). Build command: `npm run build`. Output: `dist`.

